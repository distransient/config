# Kel's TERMINAL !!MULTIPLEXER!! configuration
# Sections
# → General
# → Keybindings
# → Interface

# ¶ General
set -g set-titles on # Enable window pane title via terminfo
set -g set-titles-string "#W" # Window pane title string
setw -g utf8 on # Expect unicode sequences in all windows
set -g status-utf8 on # Treat top-bit-set characters in status string as utf-8
set -g mouse-utf8 on # Request unicode mouse input on unicode terminals
setw -g mode-mouse on # Mouse active in all modes
set -g mouse-select-pane on # Allow mouse to change pane focus
set -g mouse-resize-pane on # Allow mouse to drag-resize panes
set -g mouse-select-window on # Change window by clicking on status pane

# ¶ Keybindings 
unbind C-b # Free normal tmux command bind
set -g prefix C-a # Screen style <C-a> binding
setw -g mode-keys vi # Use vi keybindings in copy mode
set -g status-keys vi # Use vi keybindings in status line command prompt
bind C-a send-prefix # Send prefix to inner window

# ¶ Interface 
set -g status-attr dim # Style status bar
set -g status-bg colour11 # Status bar background
set -g status-fg black # Status bar text color
set -g status-left "" # Text on left side of status bar
set -g status-left-length 0 # Max left-side length
set -g status-right "#H:#S " # Text on right side of status bar
set -g status-right-length 60 # Max right-side length
setw -g monitor-activity on # Highlight windows with activity in status line
set -g pane-border-bg colour11
set -g pane-border-fg black
set -g pane-active-border-bg black  
set -g pane-active-border-fg colour13
setw -g window-status-bell-attr italics
setw -g window-status-bell-bg colour9
setw -g window-status-content-attr italics
setw -g window-status-content-bg white
setw -g window-status-activity-attr italics
setw -g window-status-activity-bg white
setw -g window-status-current-bg colour13
setw -g window-status-current-format " #I #W "
setw -g window-status-format " #I #W "
